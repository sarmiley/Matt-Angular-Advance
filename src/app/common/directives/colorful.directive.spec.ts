import { ColorfulDirective } from './colorful.directive';

describe('HoverColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorfulDirective();
    expect(directive).toBeTruthy();
  });
});
